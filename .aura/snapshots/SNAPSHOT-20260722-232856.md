# AuraKit Snapshot
- Timestamp: 2026-07-22T09:37:50Z
- Mode: REFACTOR
- Original Request: invest_wiki 赛道数据字段结构化改造 — competition/key_trends/sources 等 6 个字段从字符串格式迁移为结构化 YAML 数组/对象，前端不再用 split() 解析
- Plan: 총 5개 파일 (index.html, build_wiki_data.py, 3개 migration script → 삭제됨)
- Session ID: fe3ac3fa-a550-40a1-927e-2b36f4cb89f5

## Completed
- [x] `L2-Wiki/赛道/*.md` — 6개 필드 구조화: `competition` → `{global, china, barriers, tech_gap}`, `key_trends` → `[{title, detail}]`, `price_conduction` → `[string]`, `key_inputs` → `[string]`, `key_customers` → `[string]`, `sources` → `[{title, summary, url}]`
- [x] `L3-网页产物/index.html` — sources 렌더링: `Array.isArray()` 분기로 구조화 데이터 우선, 구 문자열 split fallback (하위호환)
- [x] `L3-网页产物/index.html` — upstream/downstream + price_conduction 렌더링도 구조화 배열 대응
- [x] `L3-网页产物/build_wiki_data.py` — YAML 구조화 필드 우선 읽기, body fallback
- [x] `python3 L3-网页产物/build_wiki_data.py` 컴파일 검증 통과 (475 노드, 66 엣지)
- [x] 임시 마이그레이션 스크립트 4개 삭제 (migrate_structured.py, migrate_phase2.py, migrate_v2.py, recover_and_migrate.py)

## Remaining
- [ ] `wikilinks` 복구 — GPU 파일은 백업에서 복구, 나머지 52개 파일 손실 (key_inputs/key_customers에서 점진적 재구축 필요)
- [ ] `companies` 복구 — 전체 손실, Research 모드로 재수집 필요
- [ ] `entity_to_dict()` 가 L0 Collection / Research / 수동편집 3개 경로에서 실제로 유일出口로 작동하는지 검증

## Last Verification
- Build: Pass (`build_wiki_data.py` — Treemap AI 40노드, 반도체 34노드, Graph 475노드/66엣지)
- Security: N/A (프론트엔드 렌더링 로직만 변경)
- Tests: N/A (자동화 테스트 없음, 수동 컴파일만)

## Key Decisions
- **구조화 우선, 문자열 fallback**: 프론트엔드는 `Array.isArray()` / `typeof === 'object'` 분기로 신규 구조화 포맷 우선 처리, 구 문자열은 기존 split 로직 유지 — 완전한 하위호환
- **Phase 1 손실 인정**: 정규식으로 YAML 문자열을 직접 수정한 Phase 1 접근이 YAML 파서를 깨뜨려 wikilinks/companies 손실. Phase 2에서 `yaml.load/dump` 기반으로 전환해 복구
- **단일 진실 공급원**: 모든 데이터 경로(L0→L2, Research→L2, 수동편집)가 `entity_to_dict()`를 통과하도록 설계 — 프론트엔드는 순수 렌더러, split 로직 제로
- **증류 불필요**: "정규식으로 YAML 깨지 마라"는 범용 엔지니어링 원칙, 독립 스킬로 캡처할 가치 없음

## Next Action
- `/aura` 실행 시 이전 CLEAN 모드(`neat-freak`) 스냅샷이 대기 중 — invest_wiki CLAUDE.md 동기화 + 아키텍처 정리 작업을 먼저 처리할지 확인
