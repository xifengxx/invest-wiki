---

# AuraKit Snapshot
- Timestamp: 2026-07-22T15:28:56Z
- Mode: REFACTOR
- Original Request: invest_wiki 赛道数据字段结构化改造 — competition/key_trends/sources/price_conduction/key_inputs/key_customers 共6个字段从旧字符串格式迁移为结构化 YAML，同步更新 schema 文档
- Plan: 2개 파일
- Session ID: fe3ac3fa-a550-40a1-927e-2b36f4cb89f5

## Completed
- [x] `L1-Schema与Pipeline/schemas/segment-schema.md` — 6개 필드 타입/포맷 수정 (key_inputs: str→list, key_customers: str→list, price_conduction: str→list, competition: str→dict, key_trends: str→list, sources: str→list)
- [x] `L1-Schema与Pipeline/schemas/field-formats.md` — v2.0 전면 개편: competition `⚔️|🔒|📐` 문자열 → `{global, china, barriers, tech_gap}` YAML dict, key_trends `1)——|` 문자열 → `[{title, detail}]` 배열, price_conduction `⇒` 문자열 → 배열, sources `\n` 분리 → `[{title, summary, url}]` 배열. 모든 필드에 YAML 예시 + 프론트엔드 렌더링 설명 포함.
- [x] Build 검증: `python3 L3-网页产物/build_wiki_data.py` → ✅ 508개 엔티티, wiki_data.json (1.3MB) 정상 생성

## Remaining
- [ ] 없음 — 모든 작업 완료

## Last Verification
- Build: Pass (508 词条, wiki_data.json 1.3MB)
- Security: N/A (문서 작업)
- Tests: N/A

## Key Decisions
- field-formats.md를 **완전히 재작성**하여 v2.0으로 버전업 — 구 문자열 포맷(`⚔️`, `⇒`, `——`, `|` 구분자)은 더 이상 유효하지 않으며, 프론트엔드도 `Array.isArray()`/`typeof === 'object'`로 구조화 데이터 우선 처리
- segment-schema.md의 필드 타입 컬럼만 수정하고, field-formats.md는 별도 파일로 분리 유지 (스키마 정의와 포맷 상세의 관심사 분리)
- L0 원시자료의 구 포맷(Collection 모드)은 그대로 유지 — ingest 단계에서 구조화 YAML로 변환

## Next Action
- 없음. 필요 시 `/lint` 실행으로 전체 Wiki 건강 상태 확인, 또는 `/collect` 실행으로 새 L0 자료 ingest
- Schema 변경사항이 LLM ingest 프롬프트(`L1-Schema与Pipeline/collector/执行指令-采集处理.md`)에 반영되었는지 후속 확인 권장
